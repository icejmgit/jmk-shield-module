# No whitespace after the comma or in your part name!
config SHIELD_JMK_LEFT
    def_bool $(shields_list_contains,jmk_left)

# No whitespace after the comma or in your part name!
config SHIELD_JMK_RIGHT
    def_bool $(shields_list_contains,jmk_right)