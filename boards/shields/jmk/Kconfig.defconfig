if SHIELD_JMK_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "jmk Keyboard"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_JMK_LEFT || SHIELD_JMK_RIGHT

config ZMK_SPLIT
    default y

endif